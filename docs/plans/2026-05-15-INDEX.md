# Code-Review Follow-Up Plans — Index

Generated 2026-05-15 from the code review on branch `feat/indicators-restructure-and-models`.

Each plan is self-contained; pick them up in any order. Plans 1 & 2 fix
correctness bugs (silent backtest bias). Plans 3-7 are pure refactors that
remove duplication without changing behaviour.

| # | Plan | Type | Priority | Est. Effort |
|---|------|------|----------|-------------|
| 1 | [Nested CV Data Leakage](2026-05-15-nested-cv-data-leakage.md) | correctness | **HIGH** | 2-3h |
| 2 | [COT/Macro Release-Lag Validation](2026-05-15-cot-macro-release-lag.md) | correctness | HIGH | 2-3h |
| 3 | [Moving Average Base Class (EMA/SMA dedup)](2026-05-15-moving-average-base-class.md) | DRY | MEDIUM | 1-2h |
| 4 | [XGBoost Base Class (mfe/rrr dedup)](2026-05-15-xgboost-base-class.md) | DRY | MEDIUM | 2-3h |
| 5 | [Trade Metrics Utility Module](2026-05-15-trade-metrics-utility.md) | DRY | MEDIUM | 2h |
| 6 | [Trade Simulation Helpers Consolidation](2026-05-15-trade-sim-consolidation.md) | DRY | MEDIUM | 2-3h |
| 7 | [Progress Tracking Unification](2026-05-15-progress-tracking-unification.md) | DRY | LOW | 3-4h |
| 8 | [FRED Monthly OECD Release-Lag](2026-05-16-fred-monthly-release-lag.md) | correctness | MEDIUM | 1-2h |

## How to execute

Each plan begins with the line:

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

Open a new session in a worktree (`/skill using-git-worktrees`), point at the
plan file, and run through the tasks. Each task ends with a commit, so
interruption is safe.

## Suggested execution order

Correctness first — bias bleeds into every backtest:

1. **Plan 1** (nested-CV leak) — single biggest impact on metric trustworthiness.
2. **Plan 2** (COT release-lag) — completes the bias audit for the data layer.

Then DRY, in order of independence:

3. **Plan 5** (metrics utility) — pre-requisite for several future changes; no other plan depends on internal data structures it touches.
4. **Plan 3** (MA base class) and **Plan 4** (XGBoost base class) — can be done in parallel; touch disjoint plugin trees.
5. **Plan 6** (trade-sim consolidation) — touches optimization callers; do after Plan 5 so the metric helpers are stable.
6. **Plan 7** (progress unification) — largest blast radius; do last, on a quiet day.

## Findings explicitly *not* covered by these plans

These items from the original review were resolved in the `fix/code-review-issues` branch and need no plan:

- CORS + credentials, API auth, path traversal in datasources, run_id/symbol validation, `_active_jobs` race, MAX_CONCURRENT_RUNS, lookahead in `resolve_tp_sl_collision`, IG credential redaction, IG order validation, plugin short-name collision, progress fraction cap, FD-leak in atomic write, MsgBus log level, lazy `ta` import, JSON size limit, `_load_manifest`/`_safe_results_path`/`safe_load_json` helpers, deepcopy in `_resolve_section`, config preset path traversal, MFE spread-at-entry consistency, xgboost_mfe `sl_variants` default.
