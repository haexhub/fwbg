config.yaml
.venv
__pycache__/
*.pyc
*.pyo
*.log
trade_history.csv
/data/
stats_export/
test_results/

# Account data (sensitive!)
accounts/

# Secrets (.env.example is the tracked template)
.env
.env.*
!.env.example
# Credentials & keys (never commit — they live forever in history)
*credential*.json
*secret*.json
secrets.yaml
secrets.yml
*.pem
*.key
*.pfx
*.p12
*.keystore
id_rsa
id_ed25519

# Build artifacts
*.egg-info/
*.egg
dist/
build/
.eggs/

# IDE
.idea/
.vscode/
*.swp
*.swo

# Type checking
.mypy_cache/

# Testing
.pytest_cache/
.coverage
htmlcov/

.claude.worktrees/

# graphify (regenerated by `graphify update .` or the post-commit hook installed via `graphify hook install`)
graphify-out/

# local-only compose override (API server workaround)
docker-compose.override.yml
